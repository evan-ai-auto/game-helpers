# Commands

> 人工操作先看本页。命令树按**业务需求**命名，任务状态只表示当前业务人工验收状态；实现方式仅作为辅助说明。
>
> 状态定义：
> - **已验收通过**：业务人工验收通过；临时人工验收通过也视为通过。
> - **部分完成**：已有实现或局部验证，但尚未完成业务人工验收。
> - **验收失败**：业务验收未达到要求，或既有验收结论已被后续证据推翻。
> - **待确认**：已有实验结果，但证据不足以完成业务验收。
> - **未验收**：尚未进行有效业务人工验收。
> - **阻塞**：存在前置问题，当前无法继续有效验收。

## 当前命令树

### 通用任务入口

`python -m game_helpers.tasks.task_workflow_cli`

```text
任务流程
├── 命魂相关任务
├── 命魂快捷图标 Hover / 运动 / 后台输入分层验证
│   ├── [部分完成] Shortcut 功能能够完整闭环
│   │   实现：完整诊断流程
│   ├── [部分完成] 能够可靠识别角色是否移动
│   │   实现：Playfield 图像差分 + OCR + RightEdge
│   ├── [部分完成] 能够可靠识别场景与地图坐标
│   │   实现：800×600 多 ROI + Windows.Media.Ocr + 字符纠错
│   ├── [部分完成] 鼠标悬停能够触发并识别 Shortcut 二级状态
│   │   实现：真实鼠标 Hover + 二级 ROI 隔离
│   ├── [部分完成] 后台消息能够触发并识别 Shortcut 悬停状态
│   │   实现：PostMessageW Hover
│   ├── [部分完成] 后台点击能够触发并确认 Shortcut 状态变化
│   │   实现：Background Click + Hotspot
│   └── [验收失败] 窗口被覆盖时仍能获得持续刷新的游戏画面
│       实现：WGC Host/WSGAME Crop + Surface 切换 + RedrawWindow
└── 道具栏检测与切换
```

### 当前快捷图标诊断子类型

```text
命魂快捷图标 Hover / 运动 / 后台输入分层验证
├── 1. 完整验证流程
├── 2. 角色运动状态验证
├── 3. 800×600 OCR 多 ROI 对照验证
├── 4. Hover 二级 ROI 隔离验证
├── 5. PostMessageW Hover 验证
├── 6. 后台 Click + Hotspot 验证
└── 7. 后台覆盖捕获新鲜度分层验证
```

> 子类型名称保留为人工入口；上方需求树才是验收对象。后续如果实现方式变化，优先修改“实现”说明，不随意改动需求名称。

## 主流程入口

完整入口、任务菜单、前置条件、输出目录和详细执行说明见 [docs/commands.md](docs/commands.md)。

主流程当前引用文件及各文件功能见 [docs/soul_task_regression.md](docs/soul_task_regression.md) 的“当前主流程引用文件”章节。

## 维护规则

### 需求与实现分离

命令树统一采用：

```text
状态 - 需求名称
    实现：当前实现方式
```

需求名称回答“要证明什么”；实现方式回答“现在怎么证明”。实现方式被推翻或替换时，需求名称原则上保持稳定。

### 最小可组合子功能

开发文件按最小可组合子功能拆分，使其可以被多个任务类型、手动验证流或子类型复用。流程文件负责组合和编排，不重复实现子功能。

### 主流程引用清单

每次主流程涉及的文件发生新增、删除、替换或职责变化时，必须同步更新主流程说明文档中的“当前主流程引用文件”列表，明确记录文件路径、文件功能、在主流程中的作用，以及被哪些任务类型/验证流使用（如适用）。

验收状态不会因为代码文件变更自动改变；只有业务人工验收结果或后续证据才能推动状态变化。
